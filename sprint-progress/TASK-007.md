# TASK-007 — P1 Dashboard L1 page

Status: DONE

## Summary
- `client/src/pages/Dashboard.jsx` — index tile, sparkline, deltas, top-10.
- Fetches via `client/src/lib/wallApi.js` → `httpClient` only.
- Empty/`index: null` branch present.
- Routed at `/` in `App.jsx` under `basename="/merchant"`.

## Evidence
- `npm run build` (client) exits 0
