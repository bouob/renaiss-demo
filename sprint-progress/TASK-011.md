# TASK-011 — P3+P4 Inventory page + L2 + Push/Hold/Clear

Status: DONE

## Summary
- `client/src/pages/Inventory.jsx` — Firebase Google sign-in gates persist; signed-out local preview still works
- Wallet scan + manual cert + cost edit + Promote/Delist/Sold via `/meta`
- L2 on-board: holdings ∩ movers
- `classifyMerchantDecision` / Detail for Push/Hold/Clear badges
- All HTTP via `inventoryApi` / `moversApi` / httpClient

## Evidence
- `npm run build` exits 0
