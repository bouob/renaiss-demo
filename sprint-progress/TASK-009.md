# TASK-009 — P2 Dashboard movers UI (Version A complete)

Status: DONE

## Summary
- `client/src/components/MoversList.jsx` + Dashboard movers section
- Fetches `/movers` via `moversApi.js` / httpClient
- Empty-state branch when movers `[]`
- Completes Version A market-side MVP shell

## Evidence
- `npm run build` exits 0
